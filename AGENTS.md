# AGENTS.md

- Keep scope narrow.
- Add only what the current step requires.
- Avoid speculative providers, keys, files, or architecture.
- Do not use the words "real", "demo", "fake", or "mock".
- Prefer exact source-backed claims over broad product language.
- Keep environment files minimal key/value only.
- Ask before expanding scope.
- Keep Elastic event payload fields stable; dynamic mappings reject later shape changes.
