substitutions:
  _IMAGE: us-central1-docker.pkg.dev/verdant-upgrade-493301-q1/fireguard/fireguard-api:manual

steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - apps/api/Dockerfile
      - -t
      - ${_IMAGE}
      - .

images:
  - ${_IMAGE}
